function rallyBadgeHtml(s){return s.stamp_rally_enabled?'<div class="rallyBadge">🎫 スタンプラリー参加</div>':''}
function rallyNoteHtml(s){return s.stamp_rally_enabled&&s.stamp_rally_note?'<div class="rallyNote"><b>スタンプ条件</b><br>'+esc(s.stamp_rally_note)+'</div>':''}
renderShops=function(){const list=filtered();$('resultCount').textContent=list.length+'件';$('shopList').innerHTML=list.length?list.map(s=>`<article class="shop ${s.stamp_rally_enabled?'rallyShop':''}" onclick="openDetail('${s.id}')">${rallyBadgeHtml(s)}<div class="cat">${esc(s.main_category)} › ${esc(s.subcategory||'その他')}</div><h3>${esc(s.name)}</h3><div class="muted">📍 ${esc(area(s.address))}</div><p>${esc(s.description||'')}</p>${rallyNoteHtml(s)}<div class="actions"><button class="btn small">詳しく見る</button><button class="btn green small" onclick="event.stopPropagation();navigateShop('${s.id}')">経路</button></div></article>`).join(''):'<div class="empty">該当する店舗・サービスがありません</div>';clusters.clearLayers();let pinCount=0;list.forEach(s=>{if(!Number.isFinite(+s.latitude)||!Number.isFinite(+s.longitude))return;let opt={};if(s.stamp_rally_enabled)opt.icon=L.divIcon({className:'',html:'<div class="rallyPin">🎫</div>',iconSize:[34,34],iconAnchor:[17,17],popupAnchor:[0,-16]});const m=L.marker([+s.latitude,+s.longitude],opt).bindPopup(`${s.stamp_rally_enabled?'<b style="color:#a34d00">🎫 スタンプラリー参加</b><br>':''}<b>${esc(s.name)}</b><br><small>${esc(s.main_category)} › ${esc(s.subcategory||'その他')}</small>${s.stamp_rally_note?`<br><small>${esc(s.stamp_rally_note)}</small>`:''}<div class="actions"><button class="btn small" onclick="openDetail('${s.id}')">詳細を見る</button></div>`);clusters.addLayer(m);pinCount++});$('mapStatus').textContent=pinCount+'件をピン表示';if(clusters.getLayers().length){const b=clusters.getBounds();if(b.isValid())map.fitBounds(b,{padding:[20,20],maxZoom:11})}}
renderRally=function(){const a=$('rallyArea'),rallyShops=shops.filter(s=>s.stamp_rally_enabled).sort((x,y)=>(x.rally_number||999)-(y.rally_number||999));const cards=rallyShops.map(s=>`<button type="button" class="rallyPreCard" onclick="openRallyShop('${s.id}')"><span class="rallyCardType">${esc(s.main_category||'その他')}</span><b>🎫 ${esc(s.name)}</b>${s.stamp_rally_note?`<span class="muted">${esc(s.stamp_rally_note)}</span>`:'<span class="muted">スタンプ対象スポット</span>'}</button>`).join('');if(!participant){a.innerHTML=`<h2>🎫 ${esc(settings.rally_title||'北海道スタンプラリー')}</h2><div class="rallyIntro"><b>来店だけでなく、通販・オンライン購入・サービス利用でも店舗確認後にスタンプを受け取れます。</b><br>飲食店・サービス・サロン・教室・相談・イベント出店などが参加できます。<br>現在の参加スポット：${rallyShops.length}件</div>${typeof publicParticipantCountHtml==='function'?publicParticipantCountHtml():''}<label for="participantEntryId"><b>希望する参加者ID（任意）</b></label><input id="participantEntryId" class="rallyInput" maxlength="80" autocomplete="off" placeholder="空欄ならMOGU IDを自動発行"><label for="nickname"><b>ニックネーム（必須）</b></label><input id="nickname" class="rallyInput" maxlength="40" autocomplete="nickname" placeholder="ニックネーム"><div class="actions"><button class="btn" onclick="startRally()">スタンプラリーに参加する</button></div><p class="muted">参加後に表示される参加者IDを、遠方購入時にお店へ伝えてください。</p><h3>🎫 スタンプ対象スポット</h3><div class="rallyPreGrid">${cards}</div>`;return}const got=progress?.stamp_count||stampIds.size,total=progress?.total_shops||rallyShops.length,entry=participant.participant_entry_id||'';a.innerHTML=`<h2>🎫 ${esc(participant.nickname)}さん</h2>${entry?`<div class="remoteIdBox"><span>遠方購入にも使える参加者ID</span><strong>${esc(entry)}</strong><button class="btn small ghost" onclick="navigator.clipboard?.writeText('${esc(entry)}');alert('参加者IDをコピーしました')">IDをコピー</button><small>通販・オンライン購入時、このIDをお店に伝えてください。</small></div>`:'<div class="notice">この参加情報には遠方購入用IDがありません。新規参加時は自動発行されます。</div>'}${typeof publicParticipantCountHtml==='function'?publicParticipantCountHtml():''}<h3>${got} / ${total}スポット</h3><div class="progress"><div style="width:${total?got/total*100:0}%"></div></div>${progress?.completed?`<div class="notice"><h2>🎉 全参加スポットクリア！</h2><strong>達成番号：${esc(progress.completion_code||'発行中')}</strong><p>${esc(settings.reward_description||'この画面を主催者へお見せください。')}</p></div>`:`<p>あと${Math.max(total-got,0)}スポット！</p>`}<p class="rallyGuide">来店時は従来どおり。遠方からの購入・オンライン利用は参加者IDを店舗へ伝え、購入確認後に店舗側からスタンプを付与してもらえます。</p><div class="stampGrid">${rallyShops.map(s=>`<button type="button" class="stamp stampTap ${stampIds.has(s.id)?'got':''}" onclick="openRallyShop('${s.id}')"><span class="rallyCardType">${esc(s.main_category||'その他')}</span><b>#${String(s.rally_number||'--').padStart(4,'0')}</b><br>${stampIds.has(s.id)?'✅ ':''}${esc(s.name)}${s.stamp_rally_note?`<span class="rallyMiniNote">${esc(s.stamp_rally_note)}</span>`:''}</button>`).join('')}</div>`}

window.openRemoteStamp=function(shopId){const s=shops.find(x=>x.id===shopId);if(!s)return;const html=`<h3>📦 遠方・オンライン購入スタンプ</h3><p class="muted">購入・サービス利用を確認した後に付与してください。お客様自身では付与できません。</p><label class="field full">お客様の参加者ID<input id="remoteParticipantId" placeholder="例：MOGU-XXXXXXXX"></label><label class="field full">店舗PIN<input id="remoteShopPin" type="password" inputmode="numeric" autocomplete="off"></label><label class="field full">メモ（任意）<input id="remoteStampNote" placeholder="通販購入・オンライン相談など"></label><div class="actions"><button class="btn" onclick="grantRemoteStamp('${shopId}')">このお客様にスタンプ付与</button></div><p id="remoteStampMsg" class="muted"></p>`;const d=$('detail');d.insertAdjacentHTML('beforeend',`<hr><div id="remoteStampPanel">${html}</div>`);$('remoteParticipantId')?.focus()}
window.grantRemoteStamp=async function(shopId){const msg=$('remoteStampMsg'),entry=$('remoteParticipantId').value.trim(),pin=$('remoteShopPin').value.trim(),note=$('remoteStampNote').value.trim();if(!entry||!pin){msg.textContent='参加者IDと店舗PINを入力してください。';return}msg.textContent='確認中…';try{const d=await api('rpc/grant_remote_participant_stamp',{method:'POST',body:JSON.stringify({p_shop_id:shopId,p_shop_pin:pin,p_participant_entry_id:entry,p_note:note})}),r=Array.isArray(d)?d[0]:d;msg.innerHTML=`✅ ${esc(r.nickname||'お客様')}さんにスタンプを付与しました。 ${r.stamp_count||0}/${r.total_shops||0}`;await loadParticipantState?.()}catch(e){msg.textContent=String(e?.message||'付与できませんでした。参加者ID・店舗PINを確認してください。')}}

const baseOpenShopEditorLinksV5=window.openShopEditor;
window.openShopEditor=function(id){const s=shops.find(x=>x.id===id);baseOpenShopEditorLinksV5(id);const form=$('shopForm');if(!form)return;const error=$('formError');if(!form.querySelector('[name="p_google_listing_url"]'))error.insertAdjacentHTML('beforebegin',`<div class="formGrid"><h3 class="field full">Google・UWリンク</h3>${field('p_google_listing_url','Google店舗ページURL',s?.google_listing_url,'url','full')}<p class="field full muted">Googleマップの店舗ページや「共有」で取得したGoogle URLを入力してください。</p>${form.querySelector('[name="p_uw_url"]')?'':field('p_uw_url','UWページURL',s?.uw_url,'url','full')}<p class="field full muted">UWページURLを入れると「UWコミュページを見る」ボタンが表示されます。</p></div>`);if(s?.stamp_rally_enabled&&!form.querySelector('.remoteStampEditorHelp'))error.insertAdjacentHTML('beforebegin',`<div class="notice remoteStampEditorHelp"><b>📦 遠方・オンライン購入にも対応</b><br>購入確認後、店舗詳細の「遠方購入にスタンプ」から、お客様の参加者ID＋店舗PINで付与できます。</div>`);form.onsubmit=saveShop};

function shopSaveErrorMessageV5(err){const raw=String(err?.message||'');let message=raw;try{message=JSON.parse(raw).message||raw}catch{}if(message.includes('Invalid PIN'))return '店舗PINまたは管理者PINが違います。';if(message.includes('Invalid Google listing URL'))return 'Google店舗ページURLは Googleマップ／share.google のURLを入力してください。';if(message.includes('Invalid URL'))return 'URLは https:// から始まる正しい形式で入力してください。';return '保存できませんでした。通信状態と入力内容を確認してください。'}
window.saveShop=async function(e){e.preventDefault();if(fallback)return $('formError').textContent='オフライン表示中は保存できません。通信後に再度お試しください。';const f=new FormData(e.currentTarget),body=Object.fromEntries(f.entries());body.p_stamp_rally_enabled=f.has('p_stamp_rally_enabled');body.p_spot_job_enabled=f.has('p_spot_job_enabled');body.p_volunteer_enabled=f.has('p_volunteer_enabled');body.p_spot_job_hourly_wage=body.p_spot_job_hourly_wage?Number(body.p_spot_job_hourly_wage):null;body.p_google_listing_url=body.p_google_listing_url||'';body.p_uw_url=body.p_uw_url||'';for(const k of Object.keys(body))if(k.endsWith('_url')&&body[k]&&!safe(body[k]))return $('formError').textContent='URLは https:// から入力してください。';const btn=e.submitter;btn.disabled=true;btn.textContent='保存中…';$('formError').textContent='';try{const d=await api('rpc/save_shop_profile_links_v2_by_pin',{method:'POST',body:JSON.stringify(body)}),updated=Array.isArray(d)?d[0]:d;if(updated){const i=shops.findIndex(x=>x.id===updated.id);if(i>=0)shops[i]=updated;}closeModal('editModal');initFilters();renderAll();openDetail(body.p_shop_id);alert('店舗情報・Google・UWリンクを保存しました。');}catch(err){$('formError').textContent=shopSaveErrorMessageV5(err);btn.disabled=false;btn.textContent='保存する';}}

const baseOpenDetailRemote=window.openDetail;
window.openDetail=function(id){baseOpenDetailRemote(id);const s=shops.find(x=>x.id===id),d=$('detail');if(!s?.stamp_rally_enabled||!d||d.querySelector('.remoteStampButton'))return;d.insertAdjacentHTML('beforeend',`<hr><div class="remoteStampButton"><h3>📦 遠方・オンラインのお客様</h3><p class="muted">通販・オンラインサービスなどの購入確認後、お店側からスタンプを付与できます。</p><button class="btn ghost" onclick="openRemoteStamp('${id}');this.closest('.remoteStampButton').remove()">遠方購入にスタンプ</button></div>`)};