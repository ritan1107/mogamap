const LINE='https://lin.ee/ZJwTkPK';const $=id=>document.getElementById(id);function pick(plan){$('selected').textContent='選択中：'+plan;$('template').textContent=`電子名刺を見ました。「${S.name}」の「${plan}」を検討しています。詳しい内容と料金について相談したいです。`;$('order').scrollIntoView({behavior:'smooth'});setTimeout(copyOrder,500)}async function copyOrder(){let t=$('template').textContent;try{await navigator.clipboard.writeText(t);toast('相談文をコピーしました')}catch(e){prompt('コピーしてください',t)}}function toast(t){let e=$('toast');e.textContent=t;e.classList.add('on');setTimeout(()=>e.classList.remove('on'),1600)}function line(){copyOrder();setTimeout(()=>window.open(LINE,'_blank'),250)}
